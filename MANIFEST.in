# Keep the test suite out of the sdist. setuptools otherwise sweeps a top-level
# tests/*.py into it by default, but not conftest.py or the ~2.5 MB of reference
# data alongside them — shipping test modules that cannot run is worse than
# shipping none. The suite runs from a repository checkout; see README.md.
prune tests
